import React from "react";

export const metadata = {
  title: "About | Hydroshark",
  description: "India's First Carbonated Hydration Drink",
};

const Layout = ({ children }) => {
  return <main>{children}</main>;
};

export default Layout;
